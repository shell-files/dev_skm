# .env.example → 복사 후 .env 로 저장하고 실제 값 입력
# cp .env.example .env

PG_HOST=localhost
PG_PORT=5432
PG_DB=your_database_name
PG_USER=your_username
PG_PASSWORD=your_password
PG_TABLE=your_table_name      # 임베딩 테이블 이름
PG_SCHEMA=public              # 스키마 (보통 public)
