server {
    listen 80;
    listen  [::]:80;
    server_name localhost;

    client_max_body_size 200M; #용량 설정

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri /index.html;
    }

    error_page 404 /index.html;
}